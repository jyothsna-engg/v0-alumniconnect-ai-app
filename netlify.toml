[build]
  command = "npm run build"  # Change this to your build command
  publish = "dist"           # Change this to your build output folder

[build.environment]
  NODE_VERSION = "20"

# Remove any Python or Ruby version specifications
